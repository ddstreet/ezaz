
- implement image-version creation with custom UEFI cert

- update vm creation --image param to reference image-version (with autocomplete)

- add 'topology' to show all objects (with filtering)

- add classes for:
  az vm list-skus (and/or az vm list-sizes)
  az vm image list-* (publisher,offer,sku,etc)

- update 'response' to format json into user-readable table/string

- fix actioncfg handler, simplify, since direct methods can/should be called
  (e.g. 'show' which manually sets actioncfg before calling do_action)

- change 'set' and 'clear', to just 'default' action
  params are obj id or '--remove' or '--none' or something to remove default

- update Config to be subclassable
- change Filter/Filters to subclass of Config

- change subscription to print warning if current != default?
  not sure, we always provide the default subscription to az cmds...
  maybe the 'current' sub doesn't make any difference, except to 'ezaz az' cmds?

- update cacheing to remove specific cache after object create/delete operation

- add object caching via class factory methods

- improve azobject parent/child creation, any class and parents should
  be creatable with just the parsed args and config defaults (or,
  action is on/by parent, e.g. list or default)

DOCS:

- FilterActionCommand
  - add better doc in help output
  - show parts correctly
  - implement showing of parent hierarchy

- add help description/submmary/etc to each subcommand
